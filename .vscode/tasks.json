{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "gcc *.c -std=c11 -lm `sdl2-config --cflags --libs` -lSDL2_image -lSDL2_mixer -lGL -lGLU -o output.bin",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}